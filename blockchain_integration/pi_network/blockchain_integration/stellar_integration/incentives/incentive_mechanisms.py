def node_operator_incentive(node_id, block_height):
    # Calculate incentive based on node ID and block height
    return 10.0

def validator_incentive(validator_id, block_height):
    # Calculate incentive based on validator ID and block height
    return 20.0
